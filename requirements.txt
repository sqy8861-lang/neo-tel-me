# Neo-tel-me 插件依赖
# ========================
# 此文件仅包含 neo-tel-me 插件所需的第三方依赖
# Neo-MoFox 框架依赖请参考主项目 requirements.txt

# 核心依赖
openai>=2.0.0
toml>=0.10.0
websockets>=11.0.3
pydantic>=2.0.0
json-repair>=0.25.0
cryptography>=41.0.0 # WebSocket HTTPS 模式生成自签名证书时需要

# 本地模式依赖（使用 PyAudio 采集音频时需要）
# 如果只在 H5 网页上使用连麦功能，可以注释掉以下依赖
# pyaudio>=0.2.14
# numpy>=1.21.0
# alibabacloud-nls-python-sdk>=1.0.0

